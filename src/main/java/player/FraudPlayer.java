package player;

import dice.Dice;

public class FraudPlayer extends Player{
    public FraudPlayer(String name, Dice typeOfDice, String modeOfDice, int totalScore) {
        super(name, typeOfDice, modeOfDice, totalScore);
    }
}
