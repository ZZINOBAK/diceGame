package player;

import dice.Dice;

public class NormalPlayer extends Player{
    public NormalPlayer(String name, Dice typeOfDice, String modeOfDice, int totalScore) {
        super(name, typeOfDice, modeOfDice, totalScore);
    }
}
